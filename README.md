# Caching with Redis and connect frontend with proxy

### Create a dotenv file and define these fields

- SERVER_PORT
- REDIS_PASSWORD
- REDIS_HOST
- REDIS_PORT

## Use:

### Both in front-end and backend run these commands:

- npm install
- npm run dev
